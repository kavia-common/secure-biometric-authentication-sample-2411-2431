<resources>
    <style name="Theme.SecureSample" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@color/colorBackground</item>

        <item name="android:textColorPrimary">@color/colorText</item>
        <item name="android:textColorSecondary">@color/colorText</item>

        <item name="android:colorAccent">@color/colorPrimary</item>
    </style>

    <style name="Widget.PrimaryButton" parent="android:Widget.Material.Button">
        <item name="android:backgroundTint">@color/colorPrimary</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Widget.SuccessButton" parent="android:Widget.Material.Button">
        <item name="android:backgroundTint">@color/colorSuccess</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Widget.ErrorButton" parent="android:Widget.Material.Button">
        <item name="android:backgroundTint">@color/colorError</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>
